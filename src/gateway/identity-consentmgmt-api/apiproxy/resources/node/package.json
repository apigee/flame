{
  "name": "identity-consent-mgmt",
  "version": "0.0.1",
  "private": true,
  "host": "{{ hostName }}",
  "apiKey": "{{ apiKey_DS }}",
  "basepath": "{{ basepath_DS }}",
  "dependencies": {
    "express": "2.5.8",
    "stylus": ">= 0.0.1",
    "jade": ">= 0.0.1"
  }
}

